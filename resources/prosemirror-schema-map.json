{
  "version": 1,
  "about": "Carve node type to ProseMirror node/mark name, for engines building a Carve <-> ProseMirror bridge. Carve type names are the normative vocabulary from the spec (docs/profiles.md); the ProseMirror names are this package's CarveKit schema. Consumers read this rather than restating the mapping, which is how carve-php once ended up emitting `citation-group` against everyone else's underscores. `pm` is the name CarveKit registers; an optional `accepts` lists further names a bridge should recognize on the way in, for concepts a stock Tiptap extension already spells differently. Where two Carve types name the same ProseMirror node or mark, the one that is a vocabulary alias of the other carries `aliasOf` naming the type that owns the name; a bridge resolving a ProseMirror name back to a Carve type must not decide that by whatever order it happens to walk this file in.",
  "_provenance": {
    "source": "markup-carve/carve-grammars tiptap/schema-map.json",
    "commit": "db1ca3ccc19055427e63e33503d0487b54c327dd",
    "refresh": "copy the complete file from carve-grammars and update commit; scripts/check-schema-map.php verifies decisions and full schema-map parity against the pin",
    "divergences": {},
    "divergencePolicy": "This vendored file must match the pinned upstream map exactly outside provenance. Change the upstream map first; local differences are not permitted.",
    "divergenceReasons": "Should one ever be needed, an entry says WHY as one of three kinds. `upstream-names-a-node` and `upstream-has-no-node` are claims about upstream: they name the ProseMirror node, and scripts/check-schema-map.php resolves that name against the published list on every run, so the reason goes false mechanically rather than when somebody rereads it. `prose` carries no such claim and stays ungated beyond the check that the difference still exists (markup-carve/carve#2270)."
  },
  "attributeNaming": "An attribute that carries a CARVE concept on a node or mark Tiptap already defines is spelled with a `carve` prefix (`carveRef`, `carveTight`, `carveKeyValues`), so it cannot collide with what a stock extension - or an application - puts on the same node. On a node this schema owns, the bare name is used: `carveCitation` has `raw`, not `carveRaw`. HTML-native names (`id`, `class`, `href`, `src`, `title`, `colspan`, `start`) keep their spelling wherever they appear. Two bridges to this model disagreed on every one of these before it was written down: carve-js wrote `ref`/`delim` where carve-php wrote `carveRef`/`tight`, so a document stored by one lost its reference spelling, its list tightness and its table spans when read by the other.",
  "positionAttribute": "Every block node may carry `carvePos`, the complete Carve `pos` object. Bridges use it to restore source-ordered definitions and definitions collected from emptied container lines. CarveKit retains it as non-rendered metadata.",
  "carveSpecVocabulary": "spec/docs/profiles.md",
  "proseMirrorSchema": "tiptap/carve-kit.js (CarveKit)",
  "types": {
    "document": {
      "kind": "node",
      "pm": "doc"
    },
    "paragraph": {
      "kind": "node",
      "pm": "paragraph",
      "attrs": {
        "carveAttrOrder": "the order the run's slots were WRITTEN in, as the AST records it: `#id`, `.class`, and each key by name",
        "carveKeyValues": "authored key=values",
        "class": "authored classes",
        "id": "authored id"
      }
    },
    "heading": {
      "kind": "node",
      "pm": "heading",
      "notes": "level plus authored id, classes, and key/value attributes",
      "attrs": {
        "carveAttrOrder": "the order the run's slots were WRITTEN in, as the AST records it: `#id`, `.class`, and each key by name",
        "carveKeyValues": "every other authored key=value, as one map",
        "class": "authored classes, space separated",
        "id": "authored id",
        "level": "heading level, 1-6"
      }
    },
    "text": {
      "kind": "node",
      "pm": "text"
    },
    "hard_break": {
      "kind": "node",
      "pm": "hardBreak"
    },
    "block_quote": {
      "kind": "node",
      "pm": "blockquote"
    },
    "code_block": {
      "kind": "node",
      "pm": "codeBlock",
      "notes": "language attr; carveLanguageRaw carries the unnormalized info string",
      "attrs": {
        "carveAttrOrder": "the order the run's slots were WRITTEN in, as the AST records it: `#id`, `.class`, and each key by name",
        "carveHeader": "fence title",
        "carveKeyValues": "authored key=values",
        "carveLabel": "fence label",
        "carveLanguageRaw": "info string as written",
        "class": "authored classes",
        "id": "authored id",
        "language": "normalized info string"
      }
    },
    "thematic_break": {
      "kind": "node",
      "pm": "horizontalRule"
    },
    "list": {
      "kind": "node",
      "pm": [
        "bulletList",
        "orderedList",
        "taskList"
      ],
      "notes": "listType selects; taskList when items carry a task marker",
      "attrs": {
        "carveAttrOrder": "the order the run's slots were WRITTEN in, as the AST records it: `#id`, `.class`, and each key by name",
        "carveBareMarker": "true for the bare `.` marker",
        "carveDelim": "marker delimiter: . or )",
        "carveKeyValues": "authored key=values",
        "carveOlType": "marker style: 1 | a | A | i | I",
        "carveTight": "false when the author separated items with blank lines - CONTENT, not styling",
        "class": "authored classes",
        "start": "first ordered number"
      }
    },
    "list_item": {
      "kind": "node",
      "pm": [
        "listItem",
        "taskItem"
      ],
      "notes": "taskItem inside a taskList",
      "attrs": {
        "carveAttrOrder": "the order the run's slots were WRITTEN in, as the AST records it: `#id`, `.class`, and each key by name",
        "carveKeyValues": "authored marker key=values",
        "checked": "task item state, taskItem only",
        "carveTaskState": "non-space task marker (`-` `_` `>` `?`), taskItem only; null for a plain `[ ]`/`[x]` item",
        "class": "authored marker classes",
        "id": "authored marker id"
      }
    },
    "table": {
      "kind": "node",
      "pm": "table"
    },
    "table_row": {
      "kind": "node",
      "pm": "tableRow"
    },
    "table_cell": {
      "kind": "node",
      "pm": [
        "tableCell",
        "tableHeader"
      ],
      "notes": "tableHeader for `|=` cells",
      "attrs": {
        "carveInheritedTextAlign": "effective alignment inherited from table column metadata; editor presentation only and never serialized as a cell override",
        "carveSpanMarker": "the span marker as written, where a cell carries one",
        "colspan": "omitted when 1",
        "rowspan": "omitted when 1",
        "textAlign": "left | center | right"
      }
    },
    "image": {
      "kind": "node",
      "pm": "image",
      "attrs": {
        "alt": "alt text",
        "carveRawRef": "the reference as written, so the spelling survives",
        "carveRef": "reference label the image resolves by",
        "src": "destination",
        "title": "title"
      }
    },
    "math": {
      "kind": "node",
      "pm": "carveMath",
      "notes": "inline atom; display attr for the `$$` form",
      "attrs": {
        "display": "true for the $$ form",
        "src": "TeX source"
      }
    },
    "footnote_ref": {
      "kind": "node",
      "pm": "carveFootnote",
      "attrs": {
        "label": "the label as written"
      }
    },
    "footnote": {
      "kind": "node",
      "pm": "carveFootnoteDefinition",
      "attrs": {
        "label": "the label as written"
      }
    },
    "div": {
      "kind": "node",
      "pm": [
        "carveDiv",
        "carveTabSet",
        "carveTab"
      ],
      "notes": "carveTabSet/carveTab are the `tabs`/`tab` classes; every other class is carveDiv",
      "attrs": {
        "carveAttrOrder": "the order the run's slots were WRITTEN in, as the AST records it: `#id`, `.class`, and each key by name",
        "carveKeyValues": "authored key=values",
        "carveTyped": "true when the KIND WORD was written on the opener (`::: note`), false when the classes came from an attribute run above a bare `:::`. One ProseMirror node serves both `div` and `admonition`, and nothing else in the document says which was authored",
        "class": "the kind word plus authored classes",
        "id": "authored id",
        "label": "tab label, carveTab only",
        "title": "fence title"
      }
    },
    "admonition": {
      "kind": "node",
      "pm": "carveDiv",
      "aliasOf": "div",
      "notes": "profile vocabulary only: an admonition is a div with a type class",
      "attrs": {
        "carveAttrOrder": "the order the run's slots were WRITTEN in, as the AST records it: `#id`, `.class`, and each key by name",
        "carveKeyValues": "authored key=values",
        "carveTyped": "true when the KIND WORD was written on the opener (`::: note`), false when the classes came from an attribute run above a bare `:::`. One ProseMirror node serves both `div` and `admonition`, and nothing else in the document says which was authored",
        "class": "the kind word plus authored classes",
        "id": "authored id",
        "label": "tab label, carveTab only",
        "title": "fence title"
      }
    },
    "directive": {
      "kind": "node",
      "pm": "carveDirective",
      "notes": "a generated-content container (CARVE-P12-057): `::: toc`, `::: footnotes`, `::: bibliography`, `::: glossary`, `::: index`, `::: references`. ITS OWN NODE, deliberately not an alias of `div` the way `admonition` is. `kind` is what tells the three named-container types apart, so a bridge going back to the AST reads the type off the node instead of carrying its own copy of the six-kind enum - and cannot emit the `admonition` whose `kind` the AST schema refuses for exactly these six words. A directive's children are usually empty: its content is generated from the document, not authored between the markers",
      "attrs": {
        "carveAttrOrder": "the order the run's slots were WRITTEN in, as the AST records it: `#id`, `.class`, and each key by name",
        "carveKeyValues": "authored key=values",
        "class": "authored classes; the kind word is NOT among them, it is `kind`",
        "id": "authored id",
        "kind": "which generated content is placed here, one of the six",
        "label": "opener `[label]` grouping id; inert in core",
        "title": "the opener's quoted title, as `carveDiv` carries an admonition's"
      }
    },
    "block_extension": {
      "kind": "node",
      "pm": "carveBlockExtension",
      "notes": "a block an extension owns (CARVE-P12-055). The REQUIRED fallback is the node's CONTENT, not an attribute: it is a real block the editor edits, and it is what the document means to a reader that does not implement the extension, so the content expression is `block` and says so. Carve 0.1 source spells none, so one reaches the editor over the AST-JSON wire only, and a canonical writer writes the fallback and reports the substitution",
      "attrs": {
        "carveAttrOrder": "the order the run's slots were WRITTEN in, as the AST records it: `#id`, `.class`, and each key by name",
        "carveKeyValues": "authored key=values",
        "class": "authored classes",
        "id": "authored id",
        "name": "the globally qualified extension name; `org.example.diagram`, never `diagram`",
        "payload": "the extension's own data as one object, `format` plus `value`; opaque to every core target and NOT rendered into HTML, for the reason `carveSubstitution` gives for its halves",
        "version": "the extension's own version, opaque here"
      }
    },
    "definition_list": {
      "kind": "node",
      "pm": "definitionList",
      "attrs": {
        "carveKeyValues": "authored key/values, which is where `{loose}` lives: PART 9 section 17 L7 leaves no blank line that could spell a definition list's looseness, so the boolean is the only spelling",
        "class": "authored classes",
        "id": "authored id"
      }
    },
    "definition_term": {
      "kind": "node",
      "pm": "definitionTerm"
    },
    "definition_description": {
      "kind": "node",
      "pm": "definitionDescription"
    },
    "figure": {
      "kind": "node",
      "pm": "carveFigure",
      "attrs": {
        "class": "authored classes",
        "id": "authored id"
      }
    },
    "figure_group": {
      "kind": "node",
      "pm": "carveFigureGroup",
      "notes": "a composite figure (PART 9 §4c): children in source order, the panels being the figure and table nodes among them. The group caption rides as a trailing carveCaption child even though it is authored below the closing fence. Deliberately no target, no title, no label - a bridge discriminates on the TYPE, never by probing for the target a figure has and this does not",
      "attrs": {
        "class": "authored classes",
        "id": "authored id"
      }
    },
    "caption": {
      "kind": "node",
      "pm": "carveCaption",
      "attrs": {
        "short": "true for the shortCaption, which never renders"
      }
    },
    "line_block": {
      "kind": "node",
      "pm": "carveLineBlock",
      "attrs": {
        "mode": "how the block preserves its leading space"
      }
    },
    "raw_block": {
      "kind": "node",
      "pm": "carveRawBlock",
      "attrs": {
        "format": "the target format"
      }
    },
    "comment": {
      "kind": "node",
      "pm": [
        "carveComment",
        "carveCommentInline"
      ],
      "notes": "block attr distinguishes %% line comments from fenced comments",
      "attrs": {
        "block": "true for a fenced comment, false for a %% line",
        "content": "legacy stored-JSON compatibility only; new carveCommentInline nodes keep editable comment text as child content",
        "delimited": "true for the `%%{ ... }%%` form (PART 9 section 21a)"
      }
    },
    "mention": {
      "kind": "node",
      "pm": [
        "carveMention",
        "carveTag"
      ],
      "accepts": [
        "mention"
      ],
      "notes": "carveTag for the `#tag` flavor. `accepts` lists names a bridge should ALSO recognize on the way in that CarveKit does not itself register: `mention` is stock tiptap/extension-mention, and refusing it made a plain editor fail on a concept Carve already models fully.",
      "attrs": {
        "id": "the name without its sigil"
      }
    },
    "inline_extension": {
      "kind": "node",
      "pm": [
        "carveInlineExtension",
        "carveEmbed"
      ],
      "notes": "carveInlineExtension is the general form - inline, name plus editable children, `:name[content]`. carveEmbed stays for a MEDIA directive, which is a block atom and cannot hold inline children; it is reached through the HTML route rather than from the AST",
      "attrs": {
        "carveAttrOrder": "the order the run's slots were WRITTEN in, as the AST records it: `#id`, `.class`, and each key by name",
        "carveKeyValues": "authored key=values",
        "class": "authored classes",
        "id": "authored id",
        "name": "the directive name"
      }
    },
    "frontmatter": {
      "kind": "node",
      "pm": "carveFrontmatter",
      "notes": "atom; content and format attrs",
      "attrs": {
        "content": "the block VERBATIM, never a parsed mapping",
        "format": "yaml, toml, ..."
      }
    },
    "link_reference_definition": {
      "kind": "node",
      "pm": "carveLinkRefDef",
      "notes": "atom; label, href and title attrs. Carrying the definition is what lets a `[text][label]` reference keep its spelling",
      "attrs": {
        "carveAttrOrder": "the order the run's slots were WRITTEN in, as the AST records it: `#id`, `.class`, and each key by name",
        "carveKeyValues": "the definition's own trailing attribute run",
        "class": "the definition's own trailing attribute run",
        "href": "destination",
        "id": "the definition's own trailing attribute run",
        "label": "the label as written",
        "title": "title"
      }
    },
    "inline_footnote": {
      "kind": "node",
      "pm": "carveInlineNote",
      "notes": "inline content; the number is a resolution artifact and is not carried",
      "attrs": {
        "carveAttrOrder": "the order the run's slots were WRITTEN in, as the AST records it: `#id`, `.class`, and each key by name",
        "carveKeyValues": "authored key=values",
        "class": "authored classes",
        "id": "authored id"
      }
    },
    "raw_inline": {
      "kind": "node",
      "pm": "carveRawInline",
      "notes": "directly editable text content with a format attr",
      "attrs": {
        "carveAttrOrder": "the order the run's slots were WRITTEN in, as the AST records it: `#id`, `.class`, and each key by name",
        "carveKeyValues": "authored key=values",
        "class": "authored classes",
        "content": "legacy stored-JSON compatibility only; new nodes keep the raw payload as child text",
        "format": "the target format",
        "id": "authored id"
      }
    },
    "literal_inline": {
      "kind": "node",
      "pm": "carveLiteral",
      "notes": "directly editable literal text content",
      "attrs": {
        "carveAttrOrder": "the order the run's slots were WRITTEN in, as the AST records it: `#id`, `.class`, and each key by name",
        "carveKeyValues": "authored key=values",
        "class": "authored classes",
        "content": "legacy stored-JSON compatibility only; new nodes keep the literal payload as child text",
        "id": "authored id"
      }
    },
    "substitution": {
      "kind": "node",
      "pm": "carveSubstitution",
      "notes": "inline atom; old and new attrs, each an inline array",
      "attrs": {
        "carveAttrOrder": "the order the run's slots were WRITTEN in, as the AST records it: `#id`, `.class`, and each key by name",
        "carveKeyValues": "authored key=values",
        "class": "authored classes",
        "id": "authored id",
        "new": "the replacement, as inline content; `[]` when the half is empty",
        "old": "the replaced text, as inline content; `[]` when the half is empty"
      }
    },
    "symbol": {
      "kind": "node",
      "pm": "carveSymbol",
      "notes": "inline atom; name attr",
      "attrs": {
        "carveAttrOrder": "the order the run's slots were WRITTEN in, as the AST records it: `#id`, `.class`, and each key by name",
        "carveKeyValues": "authored key=values",
        "class": "authored classes",
        "id": "authored id",
        "name": "the shortcode name, without its colons"
      }
    },
    "ruby": {
      "kind": "node",
      "pm": "carveRuby",
      "notes": "an interchange-only ruby annotation (CARVE-P12-054); inline ATOM, because `pairs` IS the association and there is no parallel `children` field a content expression could stand in for. A pair carries no type, attrs or pos of its own. Carve 0.1 source spells no ruby, so one arrives over the AST-JSON wire only, and a canonical writer flattens each pair to its base followed by its annotation in parentheses",
      "attrs": {
        "carveAttrOrder": "the order the run's slots were WRITTEN in, as the AST records it: `#id`, `.class`, and each key by name",
        "carveKeyValues": "authored key=values",
        "class": "authored classes",
        "id": "authored id",
        "pairs": "per pair: `base` and `annotation`, each as an INLINE ARRAY, in reading order; an annotation may be empty, a base may not"
      }
    },
    "citation_group": {
      "kind": "node",
      "pm": "carveCitation",
      "notes": "inline atom; raw and integral attrs, plus items whose prefix/locator/suffix are ProseMirror inline arrays",
      "attrs": {
        "carveAttrOrder": "the order the run's slots were WRITTEN in, as the AST records it: `#id`, `.class`, and each key by name",
        "carveKeyValues": "authored key=values",
        "class": "authored classes",
        "id": "authored id",
        "integral": "true for the `[+@key]` form",
        "items": "per item: key, suppressAuthor, and prefix/locator/suffix as INLINE ARRAYS",
        "raw": "the group as written"
      }
    },
    "heading_ref": {
      "kind": "node",
      "pm": "carveCrossref",
      "notes": "inline atom; target attr. The resolved href is a resolution artifact and is not carried",
      "attrs": {
        "carveAttrOrder": "the order the run's slots were WRITTEN in, as the AST records it: `#id`, `.class`, and each key by name",
        "carveKeyValues": "authored key=values",
        "class": "authored classes",
        "id": "authored id",
        "target": "the target as written; the resolved href is a resolution artifact and is not carried"
      }
    },
    "section": {
      "kind": "node",
      "pm": "carveSection",
      "notes": "a rendering wrapper: the bridge hoists its children through a carveSection node so the heading and body survive"
    },
    "strong": {
      "kind": "mark",
      "pm": "bold"
    },
    "emphasis": {
      "kind": "mark",
      "pm": "italic"
    },
    "underline": {
      "kind": "mark",
      "pm": "underline"
    },
    "strike": {
      "kind": "mark",
      "pm": "strike"
    },
    "code": {
      "kind": "mark",
      "pm": "code",
      "notes": "an attribute run on inline code (`` `x`{.cls} ``) belongs to the code mark; the stock Tiptap mark declares no attributes, so a bridge has to add these",
      "attrs": {
        "carveAttrOrder": "the order the run's slots were WRITTEN in, as the AST records it: `#id`, `.class`, and each key by name",
        "carveKeyValues": "authored key=values",
        "class": "authored classes",
        "id": "authored id"
      }
    },
    "highlight": {
      "kind": "mark",
      "pm": "highlight"
    },
    "subscript": {
      "kind": "mark",
      "pm": "subscript"
    },
    "superscript": {
      "kind": "mark",
      "pm": "superscript"
    },
    "small_caps": {
      "kind": "mark",
      "pm": "carveSmallCaps",
      "notes": "an interchange-only small-caps wrapper (CARVE-P12-050). A MARK, not a node: it wraps inline content and nests with every other mark, where a node would make it an atom and stop the text inside being edited. Its own attribute run does NOT ride here - CARVE-P12-050 asks a canonical writer to keep it on an ordinary attributed span around the same children, so the bridge adds a `carveSpan` mark for it and this mark carries the small-caps distinction alone. Carve 0.1 source spells no small caps, so one arrives over the AST-JSON wire only, and a canonical writer drops the wrapper, leaves the letter case alone and reports the loss"
    },
    "insert": {
      "kind": "mark",
      "pm": "carveInsert"
    },
    "delete": {
      "kind": "mark",
      "pm": "carveDelete"
    },
    "critic_comment": {
      "kind": "mark",
      "pm": "carveCriticComment",
      "notes": "content is literal, so it rides on a text node under the mark - the same shape `code` uses"
    },
    "span": {
      "kind": "mark",
      "pm": "carveSpan",
      "notes": "carries id, class and extra attributes. Stock tiptap/extension-text-style emits `textStyle`, deliberately NOT accepted: it is a carrier mark for color/fontFamily/fontSize, so mapping it here would put presentational attributes into a Carve document. An application that wants that registers `textStyle` itself.",
      "attrs": {
        "carveAttrOrder": "the order the run's slots were WRITTEN in, as the AST records it: `#id`, `.class`, and each key by name",
        "carveKeyValues": "authored key=values",
        "class": "authored classes",
        "id": "authored id"
      }
    },
    "abbreviation": {
      "kind": "mark",
      "pm": "carveAbbreviation",
      "attrs": {
        "title": "the expansion"
      }
    },
    "link": {
      "kind": "mark",
      "pm": "link",
      "attrs": {
        "carveAttrOrder": "the order the run's slots were WRITTEN in, as the AST records it: `#id`, `.class`, and each key by name",
        "carveAutolink": "true for the `<https://e.com>` form",
        "carveKeyValues": "authored key=values",
        "carveRawRef": "the reference as written",
        "carveRef": "reference label the link resolves by",
        "carveReferenceDefinition": "the definition line, where one was authored with attributes",
        "class": "authored classes",
        "href": "destination",
        "id": "authored id",
        "title": "title"
      }
    },
    "autolink": {
      "kind": "mark",
      "pm": "link",
      "aliasOf": "link",
      "notes": "profile vocabulary only: an autolink is a link whose text is its destination"
    },
    "citation_definition": {
      "kind": "node",
      "pm": "carveCitationDefinition",
      "notes": "block; `key` plus the entry's INLINE content as children, shaped after `link_reference_definition` per PART 12 section 18. New spec vocabulary at carve b6917ab that no engine parse produces yet, so it reaches the editor only over the AST-JSON wire",
      "attrs": {
        "carveAttrOrder": "the order the run's slots were WRITTEN in, as the AST records it: `#id`, `.class`, and each key by name",
        "carveKeyValues": "the leading `{author= year=}` metadata block; `author` and `year` feed author-date mode",
        "class": "authored classes",
        "id": "authored id",
        "key": "the citation key as written, without the `@`; the same string `citation.key` carries at the use site"
      }
    },
    "abbreviation_def": {
      "kind": "node",
      "pm": "carveAbbreviationDefinition",
      "attrs": {
        "abbr": "the defined term",
        "expansion": "the authored expansion"
      }
    }
  },
  "unmapped": {
    "caption_number": "numbered captions are a resolution artifact, not editor content",
    "citation": "a citation is one item in citation_group and rides in carveCitation's items attribute rather than becoming its own ProseMirror node",
    "raw_text": "raw text is the payload of a raw block, not a node an editor holds",
    "escaped_text": "escaping is a source-level concern; the editor holds the escaped character as text",
    "smart_punctuation": "smart-typography output is lossy on reparse, so it is not modeled",
    "soft_break": "a soft break is whitespace in the ProseMirror model",
    "non_breaking_space": "a generated space becomes literal U+00A0 text; its source spelling is re-derived on write"
  },
  "preservationNodes": {
    "about": "Not Carve types: the two nodes a bridge writes when it has no editable node for a construct. They are PART OF THE WIRE, not an implementation detail of one bridge - carve-grammars produced them for 179 of 1025 corpus documents and carve-php refused every one, because an unknown ProseMirror name is an error rather than a skip. A bridge reading one writes `carveSource` back as Carve source, verbatim; a bridge that cannot must say so rather than drop the node.",
    "carveUnsupported": {
      "kind": "node",
      "group": "block",
      "attrs": {
        "carveSource": "the construct as written, exactly; written back verbatim",
        "carveType": "the Carve node type the atom stands in for, so a caller can say WHAT was preserved"
      }
    },
    "carveUnsupportedInline": {
      "kind": "node",
      "group": "inline",
      "attrs": {
        "carveSource": "the construct as written, exactly; written back verbatim",
        "carveType": "the Carve node type the atom stands in for"
      }
    }
  },
  "markCarrierNodes": {
    "about": "Not a Carve type either: the node a bridge writes for a MARK WITH NO CONTENT. A ProseMirror mark cannot span zero characters, and four Carve constructs parse to a mark-producing node with no children - `[](https://example.com)`, `[]{.a}`, `{++}` and `{--}`. Walking their children produces nothing, so before this node existed each one simply disappeared: a document that was only an empty-label link came back EMPTY, and nothing reported it. A bridge writes this atom instead and reads it back as the construct it names; a bridge that cannot must say so through its dropped/degraded report rather than drop the node.",
    "carveEmptyMark": {
      "kind": "node",
      "group": "inline",
      "attrs": {
        "markAttrs": "the mark's own attributes, as one map: a link's `href`/`title` and any authored run, a span's id/class/key-values. Absent where the mark had none",
        "markType": "the ProseMirror mark this atom stands for: `link`, `carveSpan`, `carveAbbreviation`, `carveInsert`, `carveDelete`, `code` or `carveSmallCaps`. The first six have a source spelling this atom writes back; a small-caps wrapper with no children has none, so it is REPORTED as dropped rather than written"
      }
    }
  }
}
